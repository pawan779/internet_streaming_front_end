import React from 'react'
import { StyleSheet, View, Text } from 'react-native'
import Header from '../../components/Header'
    
const CreateScreen = () => {
    return (
    <View>
    <Header headerTitle="Add new Movie" back/>
    </View>
      );
};
    
const styles = StyleSheet.create({});
export default  CreateScreen;